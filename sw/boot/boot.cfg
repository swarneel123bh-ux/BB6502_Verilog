MEMORY {
    ROM:   start = $8100, size = $7EF8, type = ro, fill = yes, fillval = $EA;
    VECT:  start = $FFF8, size = $0008, type = ro, fill = yes;
}

SEGMENTS {
    CODE:    load = ROM,  type = ro;
    VECTORS: load = VECT, type = ro;
}
