MEMORY {
    ROM:   start = $8100, size = $7EFA, type = ro, fill = yes, fillval = $EA;
    VECT:  start = $FFFA, size = $0006, type = ro, fill = yes;
}

SEGMENTS {
    CODE:    load = ROM,  type = ro;
    VECTORS: load = VECT, type = ro;
}
